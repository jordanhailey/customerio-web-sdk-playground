<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ meta_title | default: "CIO JS SDK" }}</title>
    <link rel="stylesheet" href="/css/style.css">
    <script type="text/javascript" src="/js/main.js"></script>
  </head>
  <body>
    <header>
      <nav>
        <ul>
          {% for item in collections.pages %}
            <li>
              <a href="{{item.permalink}}">{{ item.link_title }}</a>
            </li>
          {% endfor %}
        </ul>
      </nav>
    </header>
    <main>
      <h1>{{ title }}</h1>
      {{ content }}
    </main>
    <footer></footer>
    {% include 'partials/cio-snippet.html' %}
  </body>
</html>