---
  permalink: /events/
  meta_title: events
  title: All the Events
  link_title: Events
  navIndex: 2
---
<section class="py-5 px-4 sm:px-6">
  <h2 class="pb-5 text-3xl sm:text-4xl font-semibold leading-7 text-gray-900">Anonymous Events</h2>
  <p class="max-w-4xl">If you have already added your site ID on
    <a href="/configuration/" class="text-teal-600 hover:text-teal-900 font-bold">the configuration page</a>
    and have JavaScript enabled in your browser—and aren't using a content 
    blocker to prevent loading any of the scripts on this page—You're being tracked.</p>
  <p class="my-4 max-w-4xl">Don't be alarmed... Unless you have scrolled down and clicked on any of the "Send Event" 
    buttons below, chances are that all we're tracking at this point is an "anonymous_id" and your page views across this domain. 
                                                                            You can read more about this by checking out our documentation on
    <a href="https://customer.io/docs/anonymous-events/" class="text-teal-600 hover:text-teal-900 font-bold">Anonymous Events</a>
  </p>
  <p class="my-4 max-w-4xl">
    Ready to track some events? Go ahead and click the button below, and we'll send an event named "<code class="text-red-500 font-mono text-sm">cio_web_sdk_test_event_1</code>" with the following metadata:
  </p>
  <pre class="my-4 max-w-4xl text-sm">
    {
      timestamp: <code class="text-red-500">&lt;the_current_epoch_timestamp_in_seconds&gt;</code>,
      message: "Testing the Customer.io Web SDK"
      source: <code class="text-red-500">&lt;the_current_window_location&gt;</code>
    }
  </pre>
  <button
    id="send-event-1"
    type="button"
    class="inline-flex items-center gap-x-1.5 rounded-md bg-purple-600 px-2.5 py-1.5 text-sm font-semibold text-white shadow-sm hover:bg-purple-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-purple-600">
    Send Event
    {% include 'partials/svg/paper-plane-icon.svg' %}
  </button>
  <blockquote class="my-5 current-identifier bg-slate-100 rounded-md shadow w-fit"></blockquote>
</section>